# template for basis kernel constructor input
#' @param columns a string vector giving the names of features on which the
#'  kernel acts. These will be used to access columns from a dataframe when the
#'  kernel is later used in a GP model.
